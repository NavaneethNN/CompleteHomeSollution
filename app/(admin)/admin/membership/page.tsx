import type { Metadata } from "next";

export const metadata: Metadata = { title: "Membership Management" };

export default function AdminMembershipPage() {
  return <main>Admin — Membership</main>;
}
